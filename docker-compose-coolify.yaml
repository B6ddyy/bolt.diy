services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-coolify
    ports:
      - "3000:5173"
    environment:
      NODE_ENV: "production"
      PORT: "5173"
    restart: unless-stopped
